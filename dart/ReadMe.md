Directory structure:

* Executables in `bin`
* Library code in `lib`
* Tests in `test`
* Input in `input`

Useful commands:

```shell
dart run bin/2017/day21.dart
dart test --reporter expanded
dart analyze
dart fix --dry-run
dart fix --apply
dart format .
```