# Setup

```shell
brew install openjdk
brew install sbt
brew install git-crypt
brew install gpg
git-crypt unlock
```

# Useful Commands

```shell
sbt scalafmtAll
sbt scalafmtSbt
sbt scalafix
sbt test
```

or

```shell
sbt build
```

